sed   
      G     - добавить интервал между строк
      -n    - запрещает вывод всех строк, вывод только по шаблону
# sed -n /root/p /etc/passwd               (только строки где есть root)
      
      q     - выход, с указаным числом отобразит количество строк в файле и выйдит 
# sed 2q /etc/passwd                       выведит 2 первые строки с файла

